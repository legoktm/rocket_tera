[package]
name = "templating"
version = "0.0.0"
edition = "2021"
publish = false

# Not a member of the crate above it; this example resolves on its own.
[workspace]

[dependencies]
rocket = "0.5.1"

[dependencies.rocket_tera]
path = "../.."
